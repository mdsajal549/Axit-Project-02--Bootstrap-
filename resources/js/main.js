// All animations will take exactly 500ms
var scroll = new SmoothScroll('a[href*="#"]', {
    speed: 1000,
    speedAsDuration: true
});



/*tooltip js*/
 $('#btn').tooltip(top)
        $('#btn-1').tooltip(top)
        $('#btn-2').tooltip(top)

